name: "Test env"

on:
  workflow_dispatch:
      
env:
      VARTEST: 'val1'
jobs: 
  build: 
    runs-on: ubuntu-latest
    steps: 
      - 
        name: "Run a one-line script"
        env:
          VARTEST: 'val2'
        run: "echo Hello, world! ${{ env.VARTEST}}"
